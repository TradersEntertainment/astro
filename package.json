{
  "name": "drama-akademi",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-fonts.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-fonts.mjs && node scripts/check-media.mjs",
    "build": "vite build",
    "postbuild": "node scripts/check-placeholders.mjs",
    "preview": "vite preview",
    "check:media": "node scripts/check-media.mjs",
    "check:placeholders": "node scripts/check-placeholders.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@fontsource-variable/figtree": "^5.2.8",
    "@fontsource/fraunces": "^5.2.8",
    "gsap": "^3.15.0"
  }
}
